/**
 * ParallaxController
 * DISABLED - No dynamic resizing or parallax effects in this project
 * All parallax and dynamic effects have been removed per project requirements
 */
export class ParallaxController {
  constructor() {
    // Parallax disabled - no dynamic resizing allowed
  }

  public refresh(): void {
    // Parallax disabled - no dynamic resizing allowed
  }
}
